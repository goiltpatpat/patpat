{
  "hooks": {
    "sessionStart": [
      {
        "command": "if [ -n \"${CURSOR_PLUGIN_ROOT:-}\" ]; then python3 \"${CURSOR_PLUGIN_ROOT}/hooks/scripts/patpat_loop_state.py\"; fi"
      }
    ],
    "beforeSubmitPrompt": [
      {
        "command": "if [ -n \"${CURSOR_PLUGIN_ROOT:-}\" ]; then python3 \"${CURSOR_PLUGIN_ROOT}/hooks/scripts/patpat_loop_state.py\"; fi"
      }
    ],
    "sessionEnd": [
      {
        "command": "if [ -n \"${CURSOR_PLUGIN_ROOT:-}\" ]; then python3 \"${CURSOR_PLUGIN_ROOT}/hooks/scripts/patpat_loop_state.py\"; fi"
      }
    ]
  }
}
